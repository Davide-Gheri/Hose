diff --git a/node_modules/i18next-conv/lib/gettext2json.js b/node_modules/i18next-conv/lib/gettext2json.js
index 5c164bf..566a4a4 100644
--- a/node_modules/i18next-conv/lib/gettext2json.js
+++ b/node_modules/i18next-conv/lib/gettext2json.js
@@ -122,8 +122,8 @@ function parseJSON(locale, data = {}, options = {}) {
           x += 1;
         }
       }
-
-      if (m !== '') targetKey = `${targetKey}${ctxSeparator}${m}`;
+      // I don't want context!
+      // if (m !== '') targetKey = `${targetKey}${ctxSeparator}${m}`;
       const values = context[key].msgstr;
 
       if (options.persistMsgIdPlural) {
